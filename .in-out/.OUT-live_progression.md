# Carnet de Bord — Projet Ordonnanceur

> **Changement de cap :** Le projet passe de "L'Armée des 12 Bots Soulseek"
> à un outil autonome d'organisation de fichiers audio : **l'Ordonnanceur**.
>
> L'utilisateur s'appelle désormais **Ordonnanceur**.

---

## 🎯 Mission

L'Ordonnanceur est un outil CLI + GUI qui analyse, organise, renomme,
dédoublonne et nettoie les fichiers audio téléchargés.

**Public :** Toute personne ayant des fichiers audio à organiser
(collectionneurs, DJs, utilisateurs de Soulseek, etc.)

**Philosophie :**
- Preview avant exécution (mode simulation par défaut)
- Opérations réversibles (dry-run/simulation)
- Résilient : chaque fichier est traité indépendamment
- Rapide : analyse de 10 000 fichiers en < 5 secondes

---

## ✅ Ce qui est construit

### Backend — `src/services/ordonnanceur_service.py` (1 789 lignes)

| Méthode | Statut | Description |
|---------|--------|-------------|
| `scanner_dossier()` | ✅ | Scan récursif, filtre audio, ignore < 100 Ko |
| `analyser_fichier()` | ✅ | Extraction tags (mutagen), fallback pattern filename |
| `analyser_dossier()` | ✅ | Analyse complète + détection doublons SHA256 |
| `preparer_renommage()` | ✅ | Génération nouveaux noms + détection conflits |
| `preparer_classement()` | ✅ | Génération chemins artiste/album + regroupement |
| `preparer_deduplication()` | ✅ | Détection doublons + résolution conflits gardes |
| `preparer_nettoyage()` | ✅ | Scan fichiers temporaires par âge + extension |
| `resoudre_conflits()` | ✅ | Résolution par suffixes _2, _3… (noms + chemins) |
| `generer_apercu()` | ✅ | Orchestre les 4 previews + résumé |
| `executer_operations()` | ✅ | Applique/simule les opérations sur le disque |
| `deplacer_vers_corbeille()` | ✅ | Corbeille dédiée avec horodatage + collision |

**Couverture tests :** 178 tests unitaires dans `TestPreparer*` + `TestExecuterOperations` + `TestDeplacerVersCorbeille`

### CLI — `src/cli_ordonnanceur.py` (432 lignes)

| Option | Description |
|--------|-------------|
| `python -m src.cli_ordonnanceur DOSSIER` | Affiche le preview complet |
| `--ops renommage classement` | Filtre les opérations |
| `--template-renommage` / `--template-classement` | Templates personnalisés |
| `--executer` | Applique réellement les opérations |
| `--dry-run` | Force la simulation |
| `--no-resoudre-conflits` | Conflits non résolus automatiquement |
| `--racine-classement` | Dossier racine de sortie |
| `--age-max` | Âge max pour le nettoyage |
| `--recursive` / `--no-recursive` | Scan récursif |

**Tests CLI :** 16 tests

### GUI — `src/gui/widgets/bots/bot_ordonnanceur.py` (1 425 lignes)

| Composant | Statut | Description |
|-----------|--------|-------------|
| Structure 4 étapes | ✅ | Choix → Aperçu → Exécution → Rapport |
| Étape 1 (Choix) | ✅ | Checkboxes, dossier browse, templates |
| Étape 2 (Aperçu) | ✅ | Preview des opérations avec données réelles |
| Étape 3 (Exécution) | ✅ | Progress bars, logs, cancellation |
| Étape 4 (Rapport) | ✅ | Tableau récapitulatif, copie presse-papier |
| Navigation | ✅ | Suivant, Précédent, Annuler |
| QThread Analyse | ✅ | `_AnalyseWorker` asynchrone avec callbacks |
| QThread Exécution | ✅ | `_OrdonnanceurWorker` asynchrone avec progression |
| Service branché | ✅ | Appels réels au service via les workers |

### Corbeille dédiée

| Fonctionnalité | Statut |
|----------------|--------|
| Dossier `~/.free-buff/corbeille/` créé à l'usage | ✅ |
| Horodatage (horodatage_nom_fichier.ext) | ✅ |
| Gestion collision avec suffixe _1, _2, _3… | ✅ |
| `shutil.move()` — sûr entre disques | ✅ |
| 10 tests de couverture (fichier vide, Unicode, etc.) | ✅ |
| `send2trash` désinstallé (non fiable) | ✅ |

### Tests globaux

| Suite | Tests | Statut |
|-------|-------|--------|
| `test_ordonnanceur_service.py` | 178 | ✅ |
| `test_ordonnanceur_service_mutagen.py` | ~10 | ✅ |
| `test_cli_ordonnanceur.py` | 16 | ✅ |
| `test_bot_ordonnanceur_integration.py` | 43 | ✅ GUI intégration |
| `test_integration_planificateur_ordonnanceur.py` | 38 | ✅ |
| Autres tests (anciens bots) | ~578 | ✅ |
| **Total** | **863** | **✅ Tout vert** |

---

## 📋 Restant (faible priorité)

| Priorité | Tâche | Dépendances |
|----------|-------|-------------|
| 🟢 1 | **Auto au démarrage** — hook dans main_window | — |

---

## 🏗️ Architecture

```
src/
├── cli_ordonnanceur.py          # CLI (argparse + preview console)
├── services/
│   └── ordonnanceur_service.py  # Backend complet (1 789 lignes)
├── gui/
│   └── widgets/
│       └── bots/
│           └── bot_ordonnanceur.py  # GUI Qt (1 425 lignes)
└── models/
    └── schemas.py               # Modèles de données

tests/
├── test_ordonnanceur_service.py           # 178 tests backend
├── test_ordonnanceur_service_mutagen.py   # Tests mutagen
├── test_cli_ordonnanceur.py              # 16 tests CLI
├── test_bot_ordonnanceur_integration.py  # 43 tests GUI
└── test_integration_planificateur_ordonnanceur.py  # 38 tests

specs/
└── bot-ordonnanceur-spec.md     # Spécification finalisée (v2)
```

### Flux de données

```
AnalyseDossier → generer_apercu() → executer_operations()
                     ↓                    ↓
              Preview (CLI/GUI)    Disque (rename/move/delete)
                                        ↓
                                deplacer_vers_corbeille()
                                    (avant delete définitif)
```

### Dépendances

- `mutagen` — lecture tags audio (ID3, Vorbis, FLAC, etc.)
- `hashlib` — SHA256 pour dédoublonnage
- `pathlib` — manipulation chemins
- `argparse` — CLI
- `PySide6` — GUI (Qt)

---

## 🐛 Bugs connus

| Bug | Statut |
|-----|--------|
| Aucun bug ouvert | ✅ |

---

## 📜 Historique des sessions

### Session 1 — Création du service backend
- `ordonnanceur_service.py` créé avec analyse, renommage, classement,
  déduplication, nettoyage
- 128 tests backend
- 783 tests globaux verts

### Session 2 — CLI Ordonnanceur
- `cli_ordonnanceur.py` avec preview console formatée
- `--executer` / `--dry-run` pour appliquer les opérations
- 16 tests CLI

### Session 3 — Conflits de noms gardés
- Détection et résolution des conflits de `filename` entre fichiers
  gardés de groupes différents
- Suffixes _2, _3… sur les groupes concernés
- Paramètre `resoudre_conflits` pour conditionner

### Session 4 — Exécution sur disque
- `executer_operations()` avec 4 helpers privés
- Simulation (dry-run) par défaut
- Propagation des paths entre sections (renommage → classement)
- 13 tests d'exécution réelle

### Session 5 — Mise à jour documentation
- Logbook réécrit pour l'Ordonnanceur
- Spec mise à jour avec statut réel
- Instructions mises à jour
- Knowledge.md créé

### Session 6 — GUI Qt branché au service
- `bot_ordonnanceur.py` : de 553 → 1 425 lignes
- Architecture 4 étapes avec QThread workers asynchrones
- `_AnalyseWorker` + `_OrdonnanceurWorker` avec signaux/slots
- Service backend réellement appelé (plus de mock)
- Barre de progression, logs, annulation, rapport

### Session 7 — Corbeille dédiée
- `deplacer_vers_corbeille()` — `shutil.move()` vers `~/.free-buff/corbeille/`
- Horodatage + gestion collisions avec suffixe _1, _2, _3
- 10 tests de couverture (fichier vide, noms Unicode, collisions multiples)
- `send2trash` désinstallé (comportement non fiable)

### Session 8 — Intégration + spec finalisée
- Test d'intégration Planificateur ↔ Ordonnanceur (38 tests)
- Spec `bot-ordonnanceur-spec.md` passée en v2 — finalisée
- Squelette de classe mis à jour avec les 28 méthodes réelles

### Session 9 — Tests GUI intégration
- `test_bot_ordonnanceur_integration.py` : 43 tests (726 lignes)
- Navigation, workers QThread, preview, rapport, cycle complet
- Tous verts (38 initiaux + 5 corrigés)

### Session 10 — Documentation finale
- README.md créé avec stats projet complètes
- Logbook mis à jour avec stats réelles (1 789 lignes service, 1 425 GUI, 863 tests)
